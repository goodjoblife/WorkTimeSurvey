.faq {
  margin-left: 1em;
}

.faq__item {
  list-style: decimal;
  line-height: 1.5em;
  margin-bottom: 20px;
  &:last-child {
    margin-bottom: 0;
  }
  @mixin font-p;
  .question {
    font-weight: 700;
  }
  .answer {
    margin-top: 7px;
  }
}
